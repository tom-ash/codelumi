export function componentDidMount() {
  const calendar = document.getElementsByClassName('react-calendar')[0]
  // calendar.addEventListener("mouseover", () => { this.props.changeControl({ calendarMouseOver: true }) })
  // calendar.addEventListener('mouseleave', () => { this.props.changeControl({ calendarMouseOver: false }) })
}