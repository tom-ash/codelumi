export function componentDidUpdate(prevProps) {
  // const { publishing: prevPublishing, authorized: prevAuthorized } = prevProps
  // const { changeApp, connecting, publishing, authorized, phoneVerified, blobs, showUserCreate, showUserAuthorize,
  //         showUserEditPhoneVerify } = this.props

  
  // if (!prevPublishing) {
  //   if (!authorized) return changeApp({ showUserCreate: true })
  //   if (!phoneVerified) return changeApp({ showUserEditPhoneVerify: true })
  // }
  // if (showUserCreate) return
  // if (showUserAuthorize) return
  // if (showUserEditPhoneVerify) return

  // if (!prevAuthorized && authorized && !phoneVerified) {
  //   return changeApp({ showUserEditPhoneVerify: true })
  // }
  // if (!authorized || !phoneVerified) return
  
  // if (!publishing) return
  // if (!connecting && !this.props.success) {
  //   if (blobs.length > 0) return this.savePicture()
  //   this.saveAnnouncement()
  // }
}
